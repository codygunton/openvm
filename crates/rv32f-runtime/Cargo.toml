[package]
name = "openvm-rv32f-runtime"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["staticlib"]

[dependencies]
# This is a C library, no Rust dependencies needed

[build-dependencies]
cc = "1.0"
